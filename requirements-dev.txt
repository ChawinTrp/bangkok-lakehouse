# Local fast loop — enough to build & test the ingestion logic without Airflow.
# (Airflow itself runs in Docker later; it does not need to be installed here.)
pandas>=2.2
pyarrow>=16        # Parquet engine for pandas
requests>=2.32     # HTTP client for the Traffy API
pytest>=8.2
ruff>=0.5          # linter + formatter
